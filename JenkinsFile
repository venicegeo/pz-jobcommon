#!/usr/bin/groovy

def THREADFIX_ID = env.THREADFIX_ID

node {
  def root = pwd()
  def mvn = tool 'M3'
  def appvers = sh(script: "git describe --long --tags --always | sed 's/\\./-/'g", returnStdout: true)

  stage('Setup') {
    deleteDir()
    //git url: "git@gitlab.gs.mil:venice/pz-jobcommon", branch: "master", credentialsId: "aaef610c-9fd0-4812-9027-755ff3a872a5"
    git url: "${env.GIT_URL}/pz-jobcommon", branch: "${env.GIT_BRANCH}"
  }

  stage('Archive') {
//    sh """
//      ${mvn}/bin/mvn clean package -U -Dmaven.repo.local=${root}
//      cp ${root}/target/pz-jobcommon-LATEST.jar ${root}/pz-jobcommon.jar
//    """
//   sh """mvn -X --settings ~/.m2/settings.xml deploy:deploy-file -Dfile=${root}/pz-jobcommon.jar \
//      -DrepositoryId=nexus \
//      -Durl="https://nexus.gs.mil/content/repositories/venice-release/" \
//      -DgroupId="org.piazza" \
//      -DgeneratePom=false \
//      -Dpackaging=jar \
//      -Dmaven.repo.local="${root}/.m2/repository" \
//      -DartifactId=pz-jobcommon \
//      -Dversion=${appvers}
//    """
  }

  stage('Scans') {
    withCredentials([[$class: 'StringBinding', credentialsId: 'THREADFIXKEY', variable: 'THREADFIX_KEY']]) {
      sh "/jslave/dependency-check/dependency-check/bin/dependency-check.sh --version"
      sh '/jslave/dependency-check/dependency-check/bin/dependency-check.sh --project "pz-jobcommon" --scan "." --format "XML" --enableExperimental --disableBundleAudit' 
      sh "/bin/curl -v --insecure -H 'Accept: application/json' -X POST --form file=@dependency-check-report.xml https://threadfix.gs.mil/rest/applications/${THREADFIX_ID}/upload?apiKey=${THREADFIX_KEY}"

      //ionConnect()
      //sh """
      //  mkdir -p ${root}/.m2/repository
      //  ${mvn}/bin/mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install \
      //    -Dmaven.repo.local=${root}/.m2/repository \
      //    -Pcoverage-per-test org.jacoco:jacoco-maven-plugin:report \
      //    -DdataFile=target/jacoco.exec
      //"""
      //sonar()

      //sh "${mvn}/bin/mvn install:install-file -Dmaven.repo.local=${root} -Dfile=pom.xml -DpomFile=pom.xml"
      sh "/opt/hp_fortify_sca/bin/sourceanalyzer -b ${env.BUILD_NUMBER} src/main/java/{*.java,**/*.java}"
      sh "/opt/hp_fortify_sca/bin/sourceanalyzer -b ${env.BUILD_NUMBER}  -scan -Xmx1G -f fortifyResults-${env.BUILD_NUMBER}.fpr"
      sh "/bin/curl -v --insecure -H 'Accept: application/json' -X POST --form file=@fortifyResults-${env.BUILD_NUMBER}.fpr https://threadfix.gs.mil/rest/applications/${THREADFIX_ID}/upload?apiKey=$THREADFIX_KEY"

    }
  }

}
